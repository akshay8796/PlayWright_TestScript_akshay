import { test, expect } from '@playwright/test';

test('test', async ({ page }) => {
});//npx playwright codegen -o tests/codegentest.spec.ts    use this command for recording mode and generate the test specific class